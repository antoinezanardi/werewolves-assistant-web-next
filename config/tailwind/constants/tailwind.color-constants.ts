import colors from "tailwindcss/colors";

const PRIMARY_COLOR = colors.blue[500];

const SECONDARY_COLOR = colors.slate[700];

const SUCCESS_COLOR = colors.emerald[500];

const ERROR_COLOR = colors.red[500];

const WARNING_COLOR = colors.yellow[500];

const INFO_COLOR = colors.cyan[600];

const DAY_COLOR = colors.yellow[500];

const NIGHT_COLOR = colors.blue[300];

const TWILIGHT_COLOR = colors.cyan[700];

export {
  PRIMARY_COLOR,
  SECONDARY_COLOR,
  SUCCESS_COLOR,
  ERROR_COLOR,
  WARNING_COLOR,
  INFO_COLOR,
  DAY_COLOR,
  NIGHT_COLOR,
  TWILIGHT_COLOR,
};