import colors from "tailwindcss/colors";

const PRIMARY_COLOR = colors.blue[500];

const SECONDARY_COLOR = colors.gray[800];

const SUCCESS_COLOR = colors.green[500];

const ERROR_COLOR = colors.red[500];

const WARNING_COLOR = colors.yellow[500];

const INFO_COLOR = colors.cyan[500];

export {
  PRIMARY_COLOR,
  SECONDARY_COLOR,
  SUCCESS_COLOR,
  ERROR_COLOR,
  WARNING_COLOR,
  INFO_COLOR,
};